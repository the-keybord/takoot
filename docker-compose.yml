version: '3.8'

services:
  takoot:
    build: .
    container_name: takoot-quiz
    restart: always
    ports:
      - "3000:3000"
    volumes:
      - takoot_data:/app/data
    environment:
      - PORT=3000
      - NODE_ENV=production

volumes:
  takoot_data:

